import React from 'react'

export const metadata = {
    title: 'Free BMI checker',
    description: 'Health Checker',
  }

export default function layout({children}) {
  return (
    <div>
        {children}
    </div>
  )
}
